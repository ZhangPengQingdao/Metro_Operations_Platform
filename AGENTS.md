# Metro Operations Platform

- 本仓库是独立的运管开放平台。原 AFC 仓库仅供业务参考，禁止源码、路径或运行时依赖回指旧仓库。
- 平台保留 Core、共享 UI、平台能力、应用宿主和管理端；业务功能通过独立应用与公开 SDK 接入。
- 修改前检查 git 状态，保留已有修改；新工作使用 `codex/` 分支。提交、推送和部署遵循用户对具体目标的授权。
- 不提交凭据、数据库、上传包、容器产物和本地运行文件。配置模板只使用占位值。
- 修改后运行相关测试、前端类型检查、SDK/前后端构建；涉及接口或启动方式时同步 docs。
- 不通过关闭检查、删除有效测试或吞异常掩盖问题。无证据的外部写结果保持未知，不自动重放。
- UI 沿用共享组件与当前中性主题；管理员身份独立于普通员工身份。

## 版本

从 0.0.1 起，小修补升第三位，较大功能升第二位，大版本升级升第一位；高位升级时低位归零。一次可交付更新统一升版，使用 `npm run version:bump -- patch|minor|major`，同步 CHANGELOG 并执行 `npm run version:check`。详见 docs/versioning.md。
