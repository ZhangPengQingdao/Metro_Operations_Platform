import type {MigrationDefinition} from '../../core/migrations/index.js';
export const businessAuthorizationMigration:MigrationDefinition={id:'app-business-authorization-expand',title:'Application owners and scoped business roles',ownerTaskId:'PLATFORM-L4-020',phase:'expand',layer:'L4',dataRows:[],migrationRows:['MIG-070'],sourceTables:[],targetTables:['platform_app_authorization','platform_app_business_roles','platform_app_business_members'],dependsOn:['app-registry-expand','platform-employee-identity-expand'],recoveryNotes:'Additive. Existing applications remain in legacy authorization until explicit activation; no automatic privilege translation.',async run({client}){await client.query(`
CREATE TABLE platform_app_authorization(installation_id uuid PRIMARY KEY REFERENCES platform_app_installations(id),owner_person_id uuid REFERENCES platform_people(id),mode text NOT NULL DEFAULT 'legacy' CHECK(mode IN ('legacy','application')),revision uuid NOT NULL,updated_at timestamptz NOT NULL DEFAULT now());
CREATE TABLE platform_app_business_roles(id uuid PRIMARY KEY,installation_id uuid NOT NULL REFERENCES platform_app_installations(id),name varchar(100) NOT NULL,status text NOT NULL CHECK(status IN ('active','inactive')),rules jsonb NOT NULL,UNIQUE(installation_id,id),UNIQUE(installation_id,name));
CREATE TABLE platform_app_business_members(installation_id uuid NOT NULL,role_id uuid NOT NULL,person_id uuid NOT NULL REFERENCES platform_people(id),PRIMARY KEY(installation_id,role_id,person_id),FOREIGN KEY(installation_id,role_id) REFERENCES platform_app_business_roles(installation_id,id));
`);}};
