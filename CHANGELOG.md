# 更新日志

## 0.6.1

- 应用安装/更新复用退出登录同款 L2 弹窗：中间显示格式或文件名，底部固定上传与解析按钮；保留签名校验、权限预览和安装确认。

- 登录与注册页面增加纵向间距，注册/返回登录改为文字入口；组织弹窗取消底部分隔线，关闭使用 L2 幽灵胶囊按钮。按当前需求移除注册验证码及挑战接口，保留限流、来源检查与管理员审核。

- 新增共享 L2 多级组织选择器：部室与工班均可直接选择，展开与选中分离，显示完整路径；用于员工注册、注册审核与基础数据组织关联字段。注册保持单列，不增加组织搜索框。
- 注册公开组织接口增加 UUID 游标分页，避免超过 100 个组织时遗漏下级；上级组织选择排除自身及后代。SDK 同步导出组件。
- 修复共享浮层定位被 inset 覆盖的问题，组织下拉继承浅深色主题并支持窄屏及模态弹窗。
- 验证：平台测试 724 项通过、9 项显式环境测试跳过，部署测试 19 项通过；严格类型检查、SDK/前后端构建与版本检查通过。浏览器验证多级组织选择、浅深色、窄屏、人员编辑，以及安装弹窗与文件选择。

## 0.6.0

- 新增员工注册申请、组织选择、一次性滑块验证与平台管理员审核；通过前不能登录，通过时确认组织和岗位，密码只存哈希，不自动授予应用权限。

- 运行状态增加未完成调用诊断；未知调用只保留证据并阻断恢复/重试，管理端显示开始时间和核对编号，不自动清除。新增运行调用上下文迁移，不记录请求载荷。

- 员工账号列表补齐手机号、企业微信 UserID，区分账号与任职状态；修复恢复在职、UserID 变更撤销验证、在职人员选择与基础资料字段边界。组织、岗位、线路、位置和设备分类目录的启停均可保存回读，编辑使用共享开关。

- 统一固定操作列表头背景；基础数据隐藏系统编码，关联字段显示名称。人员编辑支持组织和岗位下拉维护，服务端拒绝不存在或停用的目标，保留管理员审计。
- 基础数据人员目录收窄为姓名、所属组织、岗位；电话、企业微信 UserID、任职状态统一在员工账号与权限中维护。岗位列表与编辑补齐说明和启停状态。

- 抽取共享 L2 DataList、TableActions、ListPagination，并通过公开 SDK 提供给应用。统一员工/管理员账号、基础数据、应用管理、授权及物料列表的间距、操作列和图文幽灵按钮。

- 平台管理员指定应用负责人；负责人在应用内创建角色、分配人员并逐操作配置本人/工班/部室/指定组织/全平台范围。岗位不自动授予应用权限，负责人不自动取得业务数据权限。
- 旧应用授权保持至负责人明确切换；授权变更带版本校验和审计，运行中的请求与托管写入复核授权快照。
- SDK 增加业务范围声明、可信上下文与资源校验；存储列表支持范围并集后分页。物料应用接入独立业务角色。
- 修复共享 L2 筛选组件缺省条件引起的重复渲染；角色弹窗支持深浅主题、窄屏与固定保存区。
- 验证：Node 22 平台测试 722 通过、9 项环境测试跳过；物料规则 19 通过，类型检查、SDK/CLI 与前后端构建、版本一致性通过。
- 本地 Docker 已签名升级物料至 0.2.2，切换独立授权；负责人无业务角色隔离、工班隔离、撤权、出库/本人修改/删除返还及保留数据升级通过。目标 Linux 服务器与正式发行仍需对应验收。

## 0.5.0

- 个人中心弹窗按基础资料、修改密码分栏，窄屏自动切换为顶部导航；登录 Logo 放大并去掉重复标题。
- 共享 L2 弹窗完整跟随深浅主题；登录页沿用已保存的外观偏好，退出登录增加确认。员工可维护手机号、企业微信 UserID 和密码，工号姓名只读；改密校验当前密码并撤销所有旧会话。

- 统一管理员与员工登录入口，账号身份决定工作区；员工复用平台侧边栏、消息与个人资料。
- 应用管理改为紧凑表格，修复通用弹窗尺寸和桌面误显示移动导航按钮；上传安装/更新使用共享文件选择样式，桌面及窄屏已视觉检查。
- 共享弹窗取消深色遮罩，沙箱弹窗联动模糊平台导航与页头；物料工具栏恢复右上角可展开搜索与新建，操作列固定在右侧并使用共享图文按钮，库存行补齐受权限和版本校验保护的资料修改。
- 消耗记录新增领用入口、本人删除返还、班组人员下拉及服务端身份核对，隐藏历史冲销与入库，移除类型列；追加领用人 ID 迁移并采用 Phosphor 修改/删除图标。
- 物料与领用人表单改为纯下拉；共享 Select 修正箭头位置，新增可输入及加减的 QuantityInput；无权操作的消耗记录显示置灰按钮。
- 工班长及物资管理员可处理本班组消耗，保留原记录归属及跨班组隔离；应用同步深色主题，本地首屏避免闪白；员工上方消息入口改为应用管理，底部保留通知。
- SDK 导出与平台同源的 L2 React 组件及样式；物料应用改为库存、消耗记录、设置三个入口，移除手动刷新，列表随查询和保存更新。
- 物料行内修改使用原记录、冲销与新记录的原子事务；支持初始入库，保留本人及工班权限校验。
- 验证：Node 22 平台测试 715 通过、9 项环境测试跳过；物料规则测试 18 通过、部署脚本测试 19 通过；类型检查、SDK 及前后端构建通过。本地签名升级、员工出库、本人记录修改和自动更新已通过浏览器验收，原始流水与升级证据保留。

## 0.4.0

- 新增 1Panel 反向代理安装模式，仅绑定本机 18080/18081，不占用已有 80/443。
- 可选应用试装环境自动准备专用 PostgreSQL、Docker 运行镜像及托管存储配置；不预装业务应用。
- HTTPS 沙箱资源使用独立监听器、60 秒一次性地址、CSP 与加载时授权复核；资源代理剥离 Cookie 和认证头。
- 新安装器采用更新器协议 v2，旧更新器明确拒绝，不隐式接管旧部署。
- 本地验证：702 项通过、9 项环境测试跳过，类型检查和前后端构建通过。发行另需 Linux 镜像 CI。
- 本版用于受控试装：高权限存储连接和宿主 Docker socket 尚未隔离为受限代理。

## 0.3.1

- 物料控件对齐 L2 样式契约；修复沙箱高度、握手时序、托管 API 清单比较及 timestamptz 存储类型。
- 本地 700 项测试通过、9 项环境测试跳过；完成真实 Docker 物料安装、权限、出入库及保留数据升级验收。

## 0.3.0

- 新增独立员工账号、应用使用授权、角色权限管理及沙箱至隔离后端的可信员工调用链；实时复核离职、撤权、应用停用与调用权限快照。
- 托管存储增加分页、原子多行写、追加结构迁移及原事务证据核对；发布者信任、精确版本审批和安装预览进入管理端。
- 新增公开 SDK/独立 CLI 分发工具与物料试装应用：出入库、库存、关联冲销、工班长设置物资管理员及委任审计，普通成员仅纠错本人出库。
- 平台/API/SDK/CLI 统一 0.3.0；物料初装 0.1.0 和追加字段升级 0.1.1 均需平台 0.3.0。
- Node 22 全量测试 699 项通过、9 项显式环境测试跳过；类型检查、SDK/CLI/前后端构建、版本检查通过；签名初装及升级包重新导出并验签。Docker 实机安装、生产资源域及受限存储管理连接尚未完成验收，当前交付为试装代码和签名包。

## 0.2.1

- 默认安装流程改为服务器一条命令拉取正式 Git 标签并执行安装器，无需手动上传 Release。
- 安装器自动读取仓库/安装包版本和可信公钥；支持显式覆盖，兼容旧包交互输入。私有仓库认证仍使用只读令牌。
- 发行流水线核对签名公钥与仓库公钥一致，并将版本信息、公钥和首次安装说明装入官方安装器包。
- 验证：18 项部署逻辑测试通过，安装脚本语法与统一版本检查通过；发行须通过全量平台检查和实际 Linux 镜像 CI。

## 0.2.0

- 新增 Ubuntu 24.04 amd64 安装器：自动准备 Docker 依赖、签名 Release 下载、独立 PostgreSQL/API 角色、数据库迁移、首位管理员及 HTTPS 服务。
- 新增宿主独立更新器、持久任务/审计、备份、迁移与健康检查；管理员系统更新页可检查、下载并确认安装。拒绝任意命令、降级、未验签工件和未知任务重放。
- 新增显式服务器恢复命令；迁移开始后不自动回退数据库或代码。签名发行流水线生成草稿 Release，基础镜像固定 digest，生产应用托管保持关闭。
- 修复部署预检读取应用注册记录、安装完成状态落盘、备份空间检查及恢复原版本后的页面状态；安装器完整核对已安装容器运行时，避免部分包不存在导致漏检。
- 恢复过程再次中断时保留原始迁移边界，禁止因恢复阶段变化而误启动旧版本。
- 验证：653 项平台测试通过、9 项环境测试跳过；14 项部署逻辑测试通过；前端类型检查、SDK/前后端构建、版本检查通过。Ubuntu CI 已通过实际镜像构建、独立 PostgreSQL 迁移、登录与前端启动验证；目标机 systemd、真实证书和完整恢复演练仍待验收。

## 0.1.0

- 托管存储新增服务身份的 UUID 单行 CRUD 与 SDK；短期 runtime 凭据、独立会话、表结构检查、跨应用隔离、写请求去重与未知结果阻断。遗留 runtime 租约可清理，未知写入证据核对入口仍待实现。

- 默认 Gateway 接入位置/设备按 ID 查询，真实目录范围授权及返回快照复核；员工权限与应用授权取交集，服务身份单独授权。
- 新增独立员工账号、8 小时会话、管理员创建/停用/密码重置后端接口；注销、停用、重置使会话失效，离职人员不可登录或调用。员工 Gateway 入口接入宿主租约和持久工作记录。

- 应用管理新增显式托管存储生命周期装配：独立管理连接、只读前置检查、签名迁移产物绑定、同结构升级与停用/保留。
- 普通 API 身份不得复用存储管理身份；未配置或前置条件不满足时明确拒绝，不自动调整数据库权限。当前存储管理执行器仍要求超级用户，非生产最小权限代理。
- 补充配置模板、数据库只读审核脚本与能力边界说明；员工前端入口、未知日常写入恢复和真实业务应用仍待实现。
- 验证：Node 22 下 650 项测试通过、9 项显式环境测试跳过；新增真实 PostgreSQL 测试另行执行通过，覆盖签名生命周期、数据保留及提交后记录失败的证据恢复（运行时使用受控替身）。严格类型检查、SDK/前后端构建、架构分层与版本检查通过；只读审核 SQL 在一次性实例执行通过。员工目录集成及会话撤销使用 PGlite 验证；独立 PostgreSQL 17/SCRAM 另验证 runtime CRUD、跨应用/平台隔离、撤权回滚、租约清理与提交后记录失败阻断。新增迁移尚未在本地实际业务数据库执行。

## 0.0.1

- 建立独立的运管开放平台仓库，分离旧业务与历史项目入口。
- 保留管理员控制台、共享 UI、平台基础能力、SDK 与签名应用安装运行。
- 新增独立数据库初始化、开发配置、版本同步工具与项目文档。
- 平台、API、SDK 从 0.0.1 起统一维护版本。
- 验证：634 项平台测试通过、8 项显式环境测试跳过；严格类型检查与前后端构建通过；独立 PostgreSQL 初始化及签名应用安装/更新 HTTP 流程通过。
